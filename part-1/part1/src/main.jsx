import ReactDOM from 'react-dom/client' // Libreria que instala las dependencias necesarias para poder funcionar en el navegador.

import App from './App' // Modulo ECMAScript.

ReactDOM.createRoot(document.getElementById('root')).render(<App />)